#!/usr/bin/env bash
# Named wrapper for the aar-profile validate owner (#153 / #194): the contract names
# `aar-profile-validate`. Thin shim to aar_profile.py validate.
exec python3 "$(dirname "$0")/aar_profile.py" validate "$@"
